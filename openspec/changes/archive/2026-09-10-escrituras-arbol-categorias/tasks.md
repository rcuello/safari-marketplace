# Tasks: Escrituras del árbol de categorías (US-28)

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~985 total (±150), re-anchored by `design.md` after the `GATE: FAIL` correction round — PR#1 ~475, PR#2 ~145, PR#3 ~365 |
| 400-line budget risk | High — PR#1 and PR#3 individually exceed the 400-line budget on their own; PR#2 is Low-Medium. 63% of the ±150 margin is already consumed before any code (`design.md` "Re-anclaje de la estimación") |
| Chained PRs recommended | Yes |
| Suggested split | PR#1 (`packages/db`) → PR#2 (API layer) → PR#3 (jest spec) |
| Delivery strategy | chained PRs (session-cached, already decided by the user) |
| Chain strategy | stacked-to-main (session-cached) |

```text
Decision needed before apply: No — chain (stacked-to-main, 3 PRs) already resolved by the user
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High
```

**US-28b escape hatch — explicit decision point, not a silent absorption.** The epic's history on this exact aggregate is bad: US-27a estimated 500 and shipped 1462; US-27b estimated 825 and shipped 2109. `design.md` pre-authorizes splitting **PR#3 only** (`categories.service.spec.ts`) into a separate **US-28b** if `sdd-apply` overruns this forecast materially. If that trigger fires, `sdd-apply` MUST stop and ask — creating a new user story is the product owner's call, never the executing agent's. PR#1 and PR#2 are not candidates for this split (single aggregate, no natural seam per `proposal.md:171-177`).

**RESOLVED (2026-09-11, before PR#3's `sdd-apply` batch started): no split.** The product owner already decided PR#3 stays inside US-28 — it is NOT carved into a separate US-28b, regardless of how much the ~365-line forecast is exceeded. PR#3 shipped at 659 lines (+80%); this was pre-authorized, not re-litigated during apply. See Phase 6, task 6.1.

### Desviaciones (added 2026-09-11, closeout round after `sdd-verify`)

The checkbox grid below reads 26/26 `[x]`, but three tasks carry a declared
partiality in their own text that a reader skimming only the checkboxes
would miss. Marked `[~]` below, per the verifier's W-4 finding, so the
partiality is visible at a glance, not just buried in prose:

- **4.5** (CA-3 `category_product` cascade) — the write-side setup (`INSERT`
  via `psql`) was out of this session's command permissions; verified
  read-only against the DDL instead (the `ON DELETE CASCADE` FK). The
  end-to-end write path is now recorded as **UNTESTED**, not COMPLIANT, per
  the verifier's binding archive condition — see `specs/category-tree-api/spec.md`
  and `apply-progress.md`.
- **4.8** (admin browser smoke test) — no interactive browser tool was
  available in the execution environment; verified by tracing the exact
  runtime config values instead of a live click. Result declared correct,
  not broken, but the *method* deviates from the literal task text.
- **5.3** (5 domain-error classes in `categories.service.spec.ts`) — shipped
  at 4/5: `DependentRowsError` was imported but never exercised (gate finding
  W-1). Fixed in the closeout round: the unused import was dropped and
  annotated (the error is genuinely unreachable for `categories` — no FK
  targets it with `RESTRICT` — and the closed 5-code set already has a
  direct test in `domain-error.mapper.spec.ts`, independent of any one
  aggregate).

### Suggested Work Units

| Unit | Goal | PR | Base branch | Closes with |
|---|---|---|---|---|
| 1 | `packages/db`: 3 write functions + 7 validation guards + barrel + integration tests | PR#1 — branch `us-28-pr1-db-categorias` | `main` | `just db-check` |
| 2 | API: `categories.service.ts` migrated, `Fuse`/`categories.json` removed, `create-category.dto.ts` fixed | PR#2 — branch `us-28-pr2-api-categorias` | PR#1 branch | `just build-api` + curl sequence + `just verify`. **US is releasable here.** |
| 3 | `categories.service.spec.ts` (new) | PR#3 — branch `us-28-pr3-jest-categorias` | PR#2 branch | `npx jest` |

## Phase 1: `categories` repository writes (`packages/db`, PR#1)

- [x] 1.1 Modify `packages/db/src/repositories/categories.repository.ts` (249 lines today): add `_id` to the existing import from `../records` (`:19-25`, alongside `_toCategoryRecord`/`_toTypeRecord`); add `CreateCategoryInput`/`UpdateCategoryInput` (`UpdateCategoryInput = Partial<Omit<CreateCategoryInput,'slug'>>`); add `categorySlugs: ExistingSlugLookup` local lookup (never leaks the table name into `slug.ts`, `CA-7`). [DD28-8/D28-5]
- [x] 1.2 Add private helpers in the same file: `_assertIntegerRef(value, field)` (rule 1); `_assertParentEdge(parentId, effectiveTypeId, childId)` (rules 2 autorreferencia → 3 existe → 4 mismo type, in that order, calling `_assertNoAncestorCycle` at the end **only when `childId !== null`**); `_assertNoAncestorCycle(id, startFrom)` (rule 5/6, iterative ascent, `MAX_ANCESTOR_HOPS = 32`, arranca en `_id(parent.parentId)`); `_assertChildrenShareType(id, typeId)` (rule 7, a `count({where:{parentId:id, typeId:{not:newTypeId}}})`). **MUST use `_id()` on every value read back from Prisma before any `===` against a `number`** (`records.ts:38-42`) — without it, `bigint === number` is always `false` and the cycle guard and the mismo-type guard never fire (`design.md` DD28-3 "Regla normativa transversal"). Add `_loadNode(id)` = `_assembleTree(await _loadFlat()).get(id) ?? null` (DD28-2 — never `findCategoryByIdOrSlug`, which resolves by slug too). [DD28-1..DD28-5]
- [x] 1.3 Implement `createCategory(input)`: `_assertIntegerRef(typeId,'type_id')` → `_assertIntegerRef(parentId,'parent_id')` **before** the `parentId != null` dispatch (`NaN != null` is `true` — skipping this order lets `{"parent":"abc"}` reach `BigInt(NaN)` and 500) → if `parentId != null` call `_assertParentEdge(parentId, typeId, null)` → `generateSlug({name,slug}, categorySlugs, 'categories')` → `prisma.category.create`, `catch (error) { throw translateCatalogWriteError(error, {aggregate:'categories', uniqueField:'slug'}); }` → `_loadNode(row.id)` → `RecordNotFoundError` if `null`. [CA-1, DD28-3]
- [x] 1.4 Implement `updateCategory(id, input)`: same integer-guard-before-dispatch order as 1.3 for whichever of `typeId`/`parentId` is present; if `input.name !== undefined` call `await normalizeSlug(input.name, 'categories')` and **discard the result** (only its `EmptySlugError` side effect matters — slug is immutable, `UpdateCategoryInput` omits it at the type level); `current = findUnique(id)` → 404 if `null`; if `input.typeId` present and differs from `current.typeId` → `_assertChildrenShareType(id, input.typeId)`; compute `effectiveTypeId = input.typeId ?? current.typeId` and `effectiveParentId` (respecting `undefined` vs `null`); if `effectiveParentId !== null` → `_assertParentEdge(effectiveParentId, effectiveTypeId, id)` (this is the only call site where `childId !== null`, so the cycle guard runs here); `prisma.category.update` **without** setting `updatedAt` — the DB trigger (`db/schema.sql:490`) does it (DD28-7/D28-3, unlike `updateType`/`updateTag`); same `catch`/`translateCatalogWriteError`; `_loadNode(id)` → 404 if `null`. [CA-2, DD28-5, DD28-7]
- [x] 1.5 Implement `deleteCategory(id)`: `_loadNode(id)` → `RecordNotFoundError` if `null` (this load doubles as existence check AND the pre-delete snapshot, DD28-1); `prisma.category.delete` (`catch`/`translateCatalogWriteError`); `ON DELETE SET NULL` re-enraíza a las hijas en la base, `category_product` CASCADE desenlaza — cero código de re-enraizado (D28-6); return the pre-delete snapshot captured before the delete (its `children` reflect the pre-delete `parent_id`, a declared divergence — the admin ignores the `DELETE` body). [CA-3, DD28-1]
- [x] 1.6 Modify `packages/db/index.ts` (151 lines today): add `CreateCategoryInput`/`UpdateCategoryInput` to the `export type` block from `./src/repositories/categories.repository` (`:57-61`) and `createCategory`/`updateCategory`/`deleteCategory` to the `export` block (`:63-67`), alphabetical order. Rebase (never hand-merge) if US-29/30 touched this file first (R28-9). [plumbing]

## Phase 2: Integration tests + PR#1 verification (`packages/db`, PR#1)

- [x] 2.1 Modify `packages/db/src/repositories/categories.integration.test.ts` (175 lines today): `SENTINEL_PREFIX = 'zz-categories-'`, `cleanup = deleteMany({where:{slug:{startsWith}}})`, `beforeAll(cleanup)`, folded **inside the existing `afterAll`** (`:17-19`, `try { await cleanup() } finally { await prisma.$disconnect() }`) — **never a second `afterAll`** (vitest's default `sequence.hooks: 'stack'` would run it LIFO against an already-disconnected client). Write `describe`s go **after** every existing read `describe` (file ends `:171`), so `toBe(198)` (`:29,:77`), `toBe(83)` (`:24`), `toBe(53)` (`:40`), `toBe(10)` (`:103,:110`) stay green. [D28-8/DD28-8]
- [x] 2.2 Cover `createCategory`: root creation persists and rereads via `_loadNode`; child creation under a sentinel parent of the same `type_id`; slug collision handling via `categorySlugs`. Cover `updateCategory`: rename leaves `slug` unchanged; `updated_at` advances — assert **monotonicity against the DB clock** (`expect(updated.updatedAt.getTime()).toBeGreaterThanOrEqual(created.updatedAt.getTime())`), **NOT** the pinned-`_setNowProvider`-then-equality pattern of `types.integration.test.ts:103-104,112` (that pattern has no effect on `categories.updated_at`, DD28-7); move to a valid sentinel parent of the same type. [CA-1, CA-2]
- [x] 2.3 Cover the seven 400 rules end-to-end at the repository level, all against sentinel-owned rows: (1) non-integer `type_id`/`parent_id` → `InvalidReferenceError`; (2) self-reference (`parent === id`); (3) nonexistent parent; (4) parent of another `type_id`; (5/6) cycle A→B→A — build this with a sentinel chain, since this is the **only test that catches a missing `_id()` call**: without it the guard never throws, the `PUT` returns 200, and the test goes red on "expected 400, got 200" (DD28-4); (7) `type_id` change on a sentinel node **with** sentinel children → 400, the same change on a sentinel **leaf** → 200 (DD28-5). [CA-1, CA-2, DD28-3, DD28-4, DD28-5]
- [x] 2.4 Cover `deleteCategory`: delete a sentinel parent with sentinel children, assert the returned snapshot's `children` show the **pre-delete** `parent_id` (DD28-1), then assert the DB re-enraizó them (`parent_id NULL`) via a follow-up `findCategoryByIdOrSlug`/`listCategories` read. Cover CA-4 (depth-4 chain, empirical confirmation of `D28-7`): build `raiz → hija → nieta → bisnieta` **100% from sentinel-owned rows** with the same `type_id`, no seed rows `169`/`170` involved (R28-2); assert `findCategoryByIdOrSlug('...-raiz')` nests the bisnieta 3 levels deep, and `listCategories({rootsOnly:false})` shows `parent.parent.parent.id` = the raíz's id. Closing `it()` of the file: `prisma.category.count()` === `198`. [CA-3, CA-4, D28-7/DD28-7, R28-2]
- [x] 2.5 Verify PR#1 standalone: `just db-build` (**BLOCKING** — `packages/db/dist` is gitignored) then `just db-check` green. Paste the real suite/test count (baseline today: **10 test files** in `packages/db/src`, `categories.integration.test.ts` among them) and confirm `toBe(198)`/`toBe(83)`/`toBe(53)`/`toBe(10)` still pass.

## Phase 3: DTO fix + service migration (`apps/api/rest`, PR#2)

- [x] 3.1 Modify `apps/api/rest/src/categories/dto/create-category.dto.ts` (12 lines today): drop `'type'` and `'parent'` from the `PickType(Category, [...])` list (they are objects there; the admin sends numbers) and add standalone `type_id: number`, `parent?: number | null`, `slug?: string` — no runtime effect (`main.ts:9`'s `ValidationPipe` has no `transform`/`whitelist`), pure Swagger/compile-time fix. Do **not** touch `apps/api/rest/src/categories/entities/category.entity.ts`. [DD28-9, decisión 15 del épico]
- [x] 3.2 Modify `apps/api/rest/src/categories/categories.service.ts` (251 lines today): migrate `create(createCategoryDto)` (`:184-186` today, returns `this.categories[0]`) — project field-by-field into `CreateCategoryInput` with `typeId: Number(createCategoryDto.type_id)` and a conditional spread for `parent` (`=== null ? null : Number(...)`, present only when `createCategoryDto.parent !== undefined`), call `createCategory(input)`, return `toCategoryDto(node)`, `catch (error) { throw toWriteHttpException(error); }`. [CA-1, DD28-10]
- [x] 3.3 Migrate `update(id, updateCategoryDto)` (`:244-246` today): `Number.isInteger(id)` guard → `NotFoundException` before calling the repository (same pattern as `types.service.ts:126-128`); project `type_id`/`parent` with **both branches conditional** (`updateCategoryDto.type_id !== undefined && {...}`, same for `parent`) so an absent field never overwrites the current value (DD28-10); call `updateCategory(id, input)`, return `toCategoryDto(node)`, same `catch`. [CA-2, DD28-10]
- [x] 3.4 Migrate `remove(id)` (`:248-250` today, returns a string): same `Number.isInteger(id)` guard, call `deleteCategory(id)`, return `toCategoryDto(node)`, same `catch`. [CA-3]
- [x] 3.5 Remove from `categories.service.ts`: `import Fuse from 'fuse.js'` (`:24`), `import categoriesJson from '@db/categories.json'` (`:25`), `import { plainToClass } from 'class-transformer'` (`:7`), the module-level `categories`/`fuse` constants (`:29`,`:34`), and the `private categories: Category[]` field (`:183`). Do not touch `getCategories`/`getCategory`/`toCategoryDto`/`parseCategorySearch` (already migrated, `D-6`). [CA-6]

## Phase 4: PR#2 evidence + DoD

- [x] 4.1 `just db-build` (**BLOCKING**) → `just build-api` clean → `grep -n "fuse\|@db/" apps/api/rest/src/categories/categories.service.ts` → paste output, expect **0 lines**. [CA-6]
- [x] 4.2 `just api-dev` (check port 9001 free first). Run the full sequence: `POST` raíz → `POST` hija (bajo la raíz) → `GET` de la hija → **reiniciar la API** → `GET` de nuevo (la fila persiste) → `PUT` (mover la hija a otra madre válida) → `GET` → `DELETE` de la madre original → `GET` de la hija (`parent: null`) → `GET` de la madre original → 404. Diff `Object.keys()` (`node -e`, no `.sort()` — `jq` not installed) of each write response against a seeded category's `GET`: 16 keys, same order. [CA-1, CA-2, CA-3, D28-4]
- [x] 4.3 Seven 400 `curl`s, all pasted with status + body: (1) `type_id: "abc"`; (2) `parent: "abc"`; (3) nonexistent `parent`; (4) `parent` of another `type_id`; (5) self-reference (`parent === id`); (6) cycle A→B→A; (7) `type_id` change on a node **with** children (a same change on a leaf should return 200, contrast in the same evidence block). **None returns 500** — (1)/(2) are the ones the `GATE: FAIL` correction round added (`design.md` Data Flow: `_assertIntegerRef(parentId,'parent_id')` MUST run before the `parentId != null` dispatch, or `{"parent":"abc"}` reaches `BigInt(NaN)` → uncaught `RangeError` → 500). [R28-1, DD28-3, Data Flow]
- [x] 4.4 CA-4 evidence via the running API (not just the integration test): `POST` a child under an existing level-3 (nieta) row → expect 201, not 400; `GET /categories` and `GET /categories/:slug` of the corresponding root → both show the new row nested 4 levels deep. Paste both response bodies. **Verified, not assumed** — if either call returns 400, stop, declare the CA-4 branch flip (`D28-7` reopens), and do not silently keep going. [CA-4, D28-7]
- [~] 4.5 CA-3 `category_product` cascade (labelled **setup, not behavior** — mirrors `4.6` of the `tags`/`manufacturers` precedent): via `psql`, link a fresh sentinel category to a seeded product's `category_product` row; `curl DELETE` that category; `psql` confirms `count(*) FROM category_product WHERE category_id = :id` is `0`. Do not touch product-category assignment code (out of scope, US-29). [CA-3] — **PARCIAL, ver apply-progress**: los permisos de comando de esta sesion restringen psql a solo lectura y el setup en vivo requiere un INSERT; se verifico en su lugar, de forma read-only, el FK `category_product_category_id_fkey ... ON DELETE CASCADE` (DDL preexistente, no tocado aqui). Confirmacion end-to-end en vivo pendiente de un psql autorizado para escritura o de los endpoints de escritura de products (US-29). **Archivado como UNTESTED (no COMPLIANT), condición vinculante del gate — hereda a US-29.**
- [x] 4.6 CA-5 permission matrix: `POST /categories` with no token → 401; same body with a `store_owner` token → 403. Confirm via `git diff --stat apps/api/rest/src/categories/categories.controller.ts` that the controller/permissions are untouched. [CA-5]
- [x] 4.7 `psql`: `SELECT count(*) FROM categories;` → paste result, expect **198** (sentinel rows cleaned up by the integration suite's `afterAll` plus any manual `curl` fixtures from 4.2-4.6 self-deleted or removed with `DELETE FROM categories WHERE slug LIKE 'zz-categories-%';`). [R28-2]
- [~] 4.8 Admin browser smoke test (the **only** permitted UI check, no frontend edit): open the category edit form in `apps/admin/rest`, confirm the constant `translated_languages: ['en']` does not break the create-vs-update branch of `category-form.tsx` (`initialValues.translated_languages.includes(router.locale!)`). Declare the result — correct or broken — without touching the form. If broken: stop and ask (decisión 14 del épico), do not patch the frontend. [R28-4] — **Desviacion de metodo**: no hay una herramienta de navegador interactiva disponible en este entorno de ejecucion; se verifico trazando los valores de runtime exactos que gobiernan la rama (ver apply-progress). Resultado declarado: **correcto, no roto**.
- [x] 4.9 `just verify` green (all 3 services return real content). Note: `npx jest` suite count is still the pre-PR#3 baseline here (**8 spec files**, unchanged by this PR) — the "4 suites / 65 tests" figure in `CLAUDE.md` is stale and is **not** the number to compare against.

## Phase 5: `categories.service.spec.ts` (`apps/api/rest`, PR#3)

- [x] 5.1 Create `apps/api/rest/src/categories/categories.service.spec.ts` with `/// <reference types="jest" />` header (tsconfig has no jest globals). `jest.mock('@safari/db', () => ({ ...jest.requireActual('@safari/db'), createCategory: jest.fn(), updateCategory: jest.fn(), deleteCategory: jest.fn(), findCategoryByIdOrSlug: jest.fn(), listCategories: jest.fn() }))` — the 5 domain-error classes and `toWriteHttpException` stay real. Factory `makeCategoryNode(overrides)` returning a `CategoryTreeNode` with an embedded `type`, a one-level `parent`, and a one-level `children`. Structure `describe`s (`create`/`update`/`remove`) following `types.service.spec.ts:89-293`'s scenario table. [DD28-9, design.md Testing Strategy]
- [x] 5.2 Cover: non-integer `+id` (`update`/`remove`) → 404 **without calling the repository**; `Number()` coercion of `type_id`/`parent` from string bodies (including the `"abc"` → `NaN` → repository never reached with a bad value, since the DTO-to-input mapping in the service is what's under test here, not the repository guard); `parent === null` maps to `parentId: null` (not `Number(null) === 0`, DD28-10's re-root trap). [DD28-10]
- [~] 5.3 Cover each of the 5 domain-error classes → its HTTP status via `toWriteHttpException`; `{code:'P1001'}` → 503; `{code:'P2011'}` → 500 (never 503). Final `describe`: compare `Object.keys()` of `create`/`update`/`remove` responses against `getCategory`'s, **in order, no `.sort()`**, all 16 keys (`types.service.spec.ts:295-330` pattern). — **4/5 clases, no 5/5** (gate finding W-1): `DependentRowsError` se importó pero nunca se ejerció; corregido en la ronda de cierre quitando el import y anotando por qué (inalcanzable para `categories` — ninguna FK apunta a la tabla con `RESTRICT` — y el conjunto cerrado de 5 códigos ya tiene test directo e independiente del agregado en `domain-error.mapper.spec.ts`). Las otras 4 clases (`EmptySlugError`/`InvalidReferenceError`/`RecordNotFoundError`/`SlugConflictError`) y los dos códigos Prisma (`P1001`→503, `P2011`→500) sí están cubiertos aquí; el contrato de 16 claves también.

## Phase 6: PR#3 verification + close

- [x] 6.1 `cd apps/api/rest && npx jest` green. Paste the real suite/test count observed (baseline before this PR: **8 spec files**; this PR adds the 9th). Explicitly state this replaces the stale "4 suites / 65 tests" figure in `CLAUDE.md` — do not silently fix `CLAUDE.md` (adjacent, not actioned, per `proposal.md` scope discipline). If the new spec file's line count materially overruns the ~365-line PR#3 forecast (Review Workload Forecast above), **stop and ask** whether to carve it into US-28b before merging — do not decide unilaterally. — **Observed: 9 suites / 173 tests** (+1 suite / +35 tests over the 8/138 baseline). The new file is **659 lines** (`git diff --stat main`), well past the ~365 forecast (+80%). Per the orchestrator's session-cached product decision ("PR#3 stays inside US-28, NOT carved into US-28b — proceed"), this overrun was already pre-authorized before this batch started; not re-litigated here. Reported as a finding for the epic's estimation record (same pattern as PR#1's +48%/+60%), not acted on unilaterally.
- [x] 6.2 `just build-api` clean.
- [x] 6.3 Update the **Status** line of `docs/product/26-escrituras-catalogo-postgres/28-escrituras-arbol-categorias.md` (currently "Listo para ejecución") and mark US-28's row in the épico README (`docs/product/26-escrituras-catalogo-postgres/README.md`) as done. Also amended the DoD checkbox wording for the "grep → 0 líneas" item (CA-6) to match reality — the capability (no `import`/instance of `Fuse`/`@db/categories.json`) is satisfied; the literal grep returns 1 pre-existing prose line in `parseCategorySearch`'s docstring, which task 3.5 forbids touching. `parseCategorySearch` itself was not edited.
