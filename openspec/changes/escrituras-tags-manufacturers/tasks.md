# Tasks: Escrituras de `tags` y `manufacturers` (US-27b)

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~1620 added (~1666 with deletions) — commit #1 ~330, #2 ~460, #3 ~335, #4 ~500 |
| 400-line budget risk | High (each API commit exceeds 400; concentrated in one new `*.service.spec.ts` file, ~320-330 lines, the lowest-review-cost material — anchored on `types.service.spec.ts` = 331 real lines, not the ~145 the US-27a design under-predicted) |
| Chained PRs recommended | Yes (already resolved as 4 sequential commits, no PRs) |
| Chain strategy | none — single branch, 4 sequential commits, push to `main` at the end |
| Decision needed before apply | No — settled: 4 sequential commits on `us-27b-escrituras-tags-manufacturers`, no stacked branches, no PRs, push to `main` after `just verify` passes |

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: none — resuelto por el usuario 2026-09-10 (4 commits secuenciales, sin ramas apiladas ni PRs)
400-line budget risk: High
```

### Suggested Work Units

| Unit | Goal | Commit | Proved green alone by |
|---|---|---|---|
| 1 | `tags` in `packages/db`: repository writes + integration tests + barrel | #1 | `just db-build` → `just db-check` |
| 2 | `tags` in the API: service + DTO + jest spec | #2 | `just db-build` (BLOCKING) → `npx jest` → `just build-api` → `curl` |
| 3 | `manufacturers` in `packages/db` | #3 | `just db-build` → `just db-check` |
| 4 | `manufacturers` in the API + end-to-end DoD evidence | #4 | `just db-build` → `npx jest` → `just build-api` → `curl` → `just verify` |

## Phase 1: `tags` in `packages/db` (commit #1)

- [x] 1.1 Modify `packages/db/src/repositories/tags.repository.ts`: add `CreateTagInput`/`UpdateTagInput` (`Partial<Omit<CreateTagInput,'slug'>>`, DD-1), `tagSlugs: ExistingSlugLookup`; `createTag` via `generateSlug`, conditional spread only for present keys (absent ⇏ `null`, DD-1/B-4); `updateTag` calls `normalizeSlug(input.name,'tags')` discarding the result only when `name !== undefined`, slug untouched, `updatedAt: now()`; `deleteTag` = `prisma.tag.delete` with no dependent count (D27b-3), Postgres cascades `product_tag`; `typeId` guard `if (input.typeId != null && !Number.isInteger(input.typeId)) throw new InvalidReferenceError('tags','type_id',input.typeId)` (DD-2). Every write: `catch (error) { throw translateCatalogWriteError(error, {aggregate:'tags', id, uniqueField:'slug'}); }`. [CA-1, CA-2, CA-3, CA-4]
- [x] 1.2 Modify `packages/db/index.ts`: barrel-export `createTag`, `updateTag`, `deleteTag`, `CreateTagInput`, `UpdateTagInput`. Rebase (never hand-merge) if US-28/29/30 touched this file first. [no CA — plumbing]
- [x] 1.3 Modify `packages/db/src/repositories/tags.integration.test.ts`: append write `describe`s **at the end of the file**, after the existing read `describe`s, with a **mandatory comment** above the first write `describe` stating that `listTags` asserts `items[0].id === 62` over an unfiltered `orderBy id desc` (`:19-20`) and any live sentinel above id 62 breaks it (DD-8.3). Sentinel `zz-tags-`; `beforeAll(cleanup)` + cleanup **folded inside the existing `afterAll`** in `try/finally`, no second `afterAll` (LIFO hooks). Cover: `createTag` persists+rereads; `updateTag` leaves slug unchanged, `updatedAt` advances via `_setNowProvider`; `updateTag({name:''})` → `EmptySlugError`, row intact; unknown id → `RecordNotFoundError`; malformed `typeId` → `InvalidReferenceError`. Desenlace test: create a sentinel tag, link it to a seeded product via `product_tag`, delete the sentinel, assert **row-scoped**: `prisma.productTag.count({ where: { tagId: sentinelId } })` === 0 — **never** a global `productTag.count()` (B-2, `products.integration.test.ts:198-237` keeps a fixture row alive all file long, files run in parallel, no `vitest.config.*`). Closing assert as the last `it()`: `prisma.tag.count()` === 10. [CA-1, CA-2, CA-3, CA-4, CA-6]
- [x] 1.4 Verify commit #1 standalone: `just db-build` then `just db-check` green — 9 files, tests above the 111 baseline; `toBe(10)` in `tags.integration.test.ts:18` stays green.

## Phase 2: `tags` in the API (commit #2)

- [x] 2.1 Modify `apps/api/rest/src/tags/dto/create-tag.dto.ts`: `PickType(Tag, ['name','slug','details','image','icon','language'])` + standalone `type_id?: number` (D27b-1) + `@IsString() @IsNotEmpty() name: string`. Do not touch `tag.entity.ts` or `update-tag.dto.ts`. [CA-4]
- [x] 2.2 Modify `apps/api/rest/src/tags/tags.service.ts`: `create`/`update`/`remove` project field-by-field into the repository inputs via conditional spread (never spread the DTO); `+id` `Number.isInteger` guard → `NotFoundException` before calling the repository (DD-10); sequential `await createTag(...)` then `await listTypes()` then `toTagDto(record, new Map(...))` (DD-7 — never `Promise.all`); `catch (error) { throw toWriteHttpException(error); }`. Remove `@db/tags.json` import, `Fuse`, `plainToClass`, `private tags`. [CA-1, CA-2, CA-3, CA-4, CA-6]
- [x] 2.3 Create `apps/api/rest/src/tags/tags.service.spec.ts`: `jest.mock('@safari/db', () => ({ ...jest.requireActual('@safari/db'), createTag: jest.fn(), updateTag: jest.fn(), deleteTag: jest.fn(), findTagBySlug: jest.fn(), listTypes: jest.fn() }))`; assert the repository input never carries `socials`/`cover_image`; `Object.keys()` of `create`/`update`/`remove` equals `getTagBySlug`'s, in order, no `.sort()`; the 5 domain-error classes → 400/404 via `toWriteHttpException`; `{code:'P1001'}` → 503; `{code:'P2011'}` → 500 (never 503, B-1); non-integer `+id` → 404 **without calling the repository**. No assertion on `ValidationPipe` behavior (N-1). `/// <reference types="jest" />` header. Template: `types.service.spec.ts:1-88`. [CA-1, CA-2, CA-3, CA-4, CA-6]
- [x] 2.4 Verify commit #2: `just db-build` (**BLOCKING** — `packages/db/dist` is gitignored, skipping it produces the misleading `TypeError: createTag is not a function`) → `cd apps/api/rest && npx jest` green (7 suites) → `just build-api` clean → `grep -n "fuse\|@db/" apps/api/rest/src/tags/tags.service.ts` → 0 lines.
- [x] 2.5 `just api-dev`; check port 9001 free before launch. `curl` sequence for `tags` only: `POST /api/tags {"name":"Oferta Verano","type_id":9,"socials":[{"icon":"x","url":"y"}],"cover_image":{"id":1}}` → 201, 9 keys, no `socials`/`cover_image` (B-3) → `GET /api/tags/oferta-verano` → kill and relaunch the API → `GET` again → `PUT /api/tags/:id {"name":"Oferta de Invierno","slug":"intento-de-cambiar"}` → slug stays `oferta-verano` → `psql SELECT updated_at FROM tags WHERE id=:id` before/after (never via `curl`, V-6) → `POST /api/tags {"name":"X","type_id":99999}` → observe and record the literal 400 message (DD-3; do **not** edit `domain-errors.ts` — if the remedy of omitting `uniqueField` from this call site's context is needed, apply it only in `tags.repository.ts` and stop-and-ask if insufficient) → `DELETE /api/tags/:id` → 200, same 9 keys → `GET /api/tags/oferta-verano` → 404. Sequence self-cleans via its own `DELETE`. Kill the `nest` process afterward. [CA-1, CA-2, CA-7]

## Phase 3: `manufacturers` in `packages/db` (commit #3)

- [x] 3.1 Modify `packages/db/src/repositories/manufacturers.repository.ts`: add `CreateManufacturerInput`/`UpdateManufacturerInput` (DD-1), `manufacturerSlugs: ExistingSlugLookup`; `createManufacturer`/`updateManufacturer` with conditional spread only for present keys — a key absent from the input MUST NOT reach `prisma.manufacturer.{create,update}`'s `data` (B-4, DD-1 normative rule); `isApproved` stays `boolean`-typed in the repository (coercion lives in the service, DD-6); `image?: Prisma.InputJsonValue` without `| null` (DD-5, `image: null` treated as absent); same `typeId` guard as `tags` (DD-2); `deleteManufacturer` = `prisma.manufacturer.delete`, no dependent count (D27b-3). `findOrCreateManufacturerBySlug` stays **untouched**. Every write: `catch (error) { throw translateCatalogWriteError(error, {aggregate:'manufacturers', id, uniqueField:'slug'}); }`. [CA-1, CA-2, CA-3, CA-4]
- [x] 3.2 Modify `packages/db/index.ts`: barrel-export `createManufacturer`, `updateManufacturer`, `deleteManufacturer`, `CreateManufacturerInput`, `UpdateManufacturerInput`. Rebase if needed. [no CA — plumbing]
- [x] 3.3 Modify `packages/db/src/repositories/manufacturers.integration.test.ts`: sentinel `zz-manu-`, `beforeAll(cleanup)` + cleanup folded inside the existing `afterAll` in `try/finally`. No file-order trap here (unlike `tags`) — new write `describe`s may follow the existing convention. Cover: `createManufacturer` persists+rereads; `updateManufacturer` leaves slug unchanged, `updatedAt` advances; `updateManufacturer({name:''})` → `EmptySlugError`; unknown id → `RecordNotFoundError`; malformed `typeId` → `InvalidReferenceError`. Desenlace test: sentinel manufacturer linked to a seeded product's `manufacturer_id`, delete it, assert **row-scoped**: `(await prisma.product.findUnique({ where: { id: probeId } })).manufacturerId` === `null` — **never** a global `product.count()` (B-2). Closing assert as the last `it()`: `prisma.manufacturer.count()` === 14. [CA-1, CA-2, CA-3, CA-4, CA-6]
- [x] 3.4 Verify commit #3 standalone: `just db-build` then `just db-check` green — 9 files, tests above commit #1's count; `manufacturers.integration.test.ts:18` `toBe(14)` stays green.

## Phase 4: `manufacturers` in the API + end-to-end DoD evidence (commit #4)

- [x] 4.1 Modify `apps/api/rest/src/manufacturers/dto/create-manufacturer.dto.ts`: `OmitType(Manufacturer, ['id','created_at','updated_at','products_count','type','translated_languages'])` (N-9 adds the two timestamps to the omit list) + `@IsString() @IsNotEmpty() name` + `@IsOptional() @IsBoolean() is_approved?: boolean` (N-2, guards `Boolean("false") === true`) + standalone `shop_id?: string` (ignored, no column). Do not touch `manufacturer.entity.ts` or `update-manufacturer.dto.ts`. [CA-4]
- [x] 4.2 Modify `apps/api/rest/src/manufacturers/manufacturers.service.ts`: `create`/`update`/`remove` project field-by-field via conditional spread (never spread the DTO, never absent → `null`, B-4); `is_approved` coercion `...(dto.is_approved !== undefined && { isApproved: Boolean(dto.is_approved) })` (DD-6); `type_id` coercion `...(dto.type_id !== undefined && { typeId: dto.type_id === null ? null : Number(dto.type_id) })` (DD-2); `socials`/`cover_image`/`language`/`shop_id` are never projected (V-1, V-2, V-3); `+id` `Number.isInteger` guard → `NotFoundException` (DD-10); sequential `await createManufacturer(...)` then `await listTypes()` then `toManufacturerDto(record, Map)` (DD-7); `Number(record.isApproved)` in the mapper stays **intact** (`:60`); `catch (error) { throw toWriteHttpException(error); }`. Remove `@db/manufacturers.json` import, `Fuse`, `private manufacturers`. [CA-1, CA-2, CA-3, CA-4, CA-6]
- [x] 4.3 Create `apps/api/rest/src/manufacturers/manufacturers.service.spec.ts`: `jest.mock('@safari/db', ...)` mocking `createManufacturer`/`updateManufacturer`/`deleteManufacturer`/`findManufacturerBySlug`/`listTypes`; assert the repository input never carries `socials`/`cover_image`/`language`/`shop_id`; **unit-level B-4 proof**: feed the 5-key toggle payload (`{id,name,is_approved,type_id,language}`) and assert the mocked `updateManufacturer` call args omit `description`/`website`/`image` entirely (not `undefined` values — absent keys); `Boolean()`/`Number()` coercion of `is_approved`; `Object.keys()` of `create`/`update`/`remove` equals `getManufacturesBySlug`'s, in order, no `.sort()` (13 keys); 5 domain-error codes → 400/404; `{code:'P1001'}` → 503; `{code:'P2011'}` → 500; non-integer `+id` → 404 without calling the repository. [CA-1, CA-2, CA-3, CA-4, CA-6]
- [x] 4.4 Verify commit #4 build: `just db-build` (**BLOCKING**) → `npx jest` green (8 suites) → `just build-api` clean → `grep -n "fuse\|@db/" apps/api/rest/src/manufacturers/manufacturers.service.ts` → 0 lines.
- [x] 4.5 `just api-dev`; check port 9001 free before launch. CA-1/CA-2 (manufacturers): `POST /api/manufacturers {"name":"Marca Prueba","type_id":9,"is_approved":true,"description":"desc-sonda","website":"https://sonda.test","image":{"id":1,"original":"o","thumbnail":"t"},"socials":[{"icon":"x","url":"y"}],"cover_image":{"id":1},"language":"es"}` → 201, 13 keys, `socials:[]`, `cover_image:null`, `is_approved:1` → kill/relaunch API → `GET` → `PUT` with the **exact 5-key toggle body** `{"name":"Marca Prueba","is_approved":false,"type_id":9,"language":"es"}` (`manufacturer-list.tsx:134-142`) → kill/relaunch mid-sequence → `GET` → `is_approved:0` → `psql` right after the `PUT`: `description = 'desc-sonda'`, `website` and `image` **intact** (B-4 proof; three `NULL` would mean the apply projected absent→`null`) → `DELETE /api/manufacturers/:id` → 200, 13 keys → `GET /api/manufacturers/marca-prueba` → 404. Sequence self-cleans via its own `DELETE`. [CA-1, CA-2]
- [x] 4.6 CA-3 setup (labelled **setup, not behavior**, D27b-7): `docker exec` `psql`: `UPDATE products SET manufacturer_id = :marca_prueba_id WHERE id = 1;` and `INSERT INTO product_tag (product_id, tag_id) VALUES (2, :oferta_verano_id);` (reuse ids from a fresh probe pair created for this step, not seeded rows). `curl DELETE /api/manufacturers/:id` and `DELETE /api/tags/:id` on those probes → 200 → `psql`: `count(*) FROM products` = 1200, `manufacturer_id` of row 1 = `NULL`, `count(*) FROM product_tag` = 0. No manual `product_tag` deletion — the FK is `ON DELETE CASCADE` toward `tags` (`schema.sql:433`), Postgres removes the links itself. If aborted before the `DELETE`s: `UPDATE products SET manufacturer_id = NULL WHERE id = 1;` and `DELETE FROM product_tag WHERE tag_id = :id;`. [CA-3]
- [x] 4.7 CA-4 full battery: `PUT`/`DELETE /api/{tags,manufacturers}/99999` → 404; `PUT /api/tags/abc` → 404 (`NaN`, repository never called); `POST {}` → 400; `POST {"name":""}` → 400; `POST {"name":"!!!"}` → 400; `PUT {"name":""}` → 400 (capa 2, row intact via `psql`); `POST /api/tags {"name":"X","type_id":99999}` → 400, message already recorded in 2.5. Collision run **literally as the scenario writes it** (`spec.md:87-90`, N-3): `POST /api/tags {"name":"Oferta"}` → 201 `slug:"oferta"` → `POST /api/tags {"name":"Oferta"}` again → 201 `slug:"oferta-2"`; `POST /api/manufacturers {"name":"Medicure"}` (seeded `medicure`, id 18) → 201 `slug:"medicure-2"`. Stack-trace check: capture the API log across the whole battery, `grep -cE '^\s+at '` → 0 (N-5). Cleanup by returned id, never `id > N`: `DELETE FROM tags WHERE id IN (:oferta_id, :oferta_2_id);` and `DELETE FROM manufacturers WHERE id = :medicure_2_id;`. [CA-4]
- [x] 4.8 CA-5 permission matrix: real tokens via `POST /api/token`. No-token (401×6) and `customer` (403×6) probes write nothing (`PermissionsGuard` cuts before the service) — any id is safe. `store_owner` on `tags` → 403×3 (`ADMIN_ONLY`, no write). `store_owner` on `manufacturers` **writes for real** (`permissions.decorator.ts:22`, `manufacturers.controller.ts:30-31,52-53,61-62`) — run as a self-cleaning sentinel sequence: `POST /api/manufacturers {"name":"Zz Manu Permisos", "description":"sonda-permisos"}` → 201 → `PUT /api/manufacturers/{returned id} {"name":"Zz Manu Permisos","description":"sonda-permisos-editada"}` → 200 → `DELETE /api/manufacturers/{same id}` → 200. **Forbidden**: `PUT`/`DELETE` of a seeded id with this token — no undo, `just db-reset` is prohibited. Abort fallback: `DELETE FROM manufacturers WHERE slug LIKE 'zz-manu-permisos%';`. `git diff` of both controllers stays empty. [CA-5]
- [x] 4.9 CA-6: `grep -n "fuse\|@db/" apps/api/rest/src/{tags,manufacturers}/*.service.ts` → 0 lines (already confirmed per-commit in 2.4/4.4, recheck combined); with `just db-check` idle, `psql` counts: `tags` = 10, `manufacturers` = 14, `product_tag` = 0, `products` = 1200 with `manufacturer_id IS NOT NULL` = 0.
- [x] 4.10 CA-7 final check: `git diff --stat packages/db/src/slug.ts packages/db/src/domain-errors.ts apps/api/rest/src/common/errors/` → empty. [CA-7]
- [x] 4.11 `just build-api` and `just verify` green (all 3 services return real content); kill any orphaned `nest start --watch` on port 9001 before and after this step. Push branch `us-27b-escrituras-tags-manufacturers` to `main`. **Build/verify executed and green; the push itself was NOT executed** — this apply run's explicit session instructions say "Do NOT commit, push or create branches"; the push is left to the orchestrator/user after review.
